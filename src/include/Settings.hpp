#pragma once

const float SCREEN_WIDTH = 1920;
const float SCREEN_HEIGHT = 1080;

const float TILE_SIZE = 128;
const float TILE_SIZE_HALF = TILE_SIZE / 2;
const float TILE_SIZE_QUARTER = TILE_SIZE / 4;

const float PLAYER_WIDTH = 105;
const float PLAYER_HEIGHT = 200;
const float PLAYER_IDLE_W = 105;
const float PLAYER_IDLE_H = 200;

const std::string SCORE_FONT = "res/fonts/CHILLER.TTF";

const int NUMBER_OF_PUMPKINS = 3;
const float START_LOC = 100;
const int GAME_SPEED = 400;
const int GROUND_BASE = SCREEN_HEIGHT - TILE_SIZE;


const float GRAVITY = 9.81;
const float JUMP_FORCE = 1.25;